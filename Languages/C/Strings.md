```
#include <string.h>
```
C does not have built in String types
Strings are arrays of characters terminated by a null character '\0'

```
char str1[100]; // Array of 100 characters
char *str2 = "Hello"; // Pointer to a string literal
```